"""Planning prompt helpers.

The current implementation still builds prompts inside node functions while the
agent graph is being modularized. Keep planning-specific builders here as the
prompt surface grows.
"""
